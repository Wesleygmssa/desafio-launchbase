

{% extends 'layout.njk' %}

{% block content%}
   {# <div class="login">
        <div class="login-logo">
            <h1> <span class="material-icons">beenhere</span> Portal do professor</h1>
        </div>
        <div class="area-login">
           <span class="material-icons">person</span><input type="text" placeholder="LOGIN" >
           <span class="material-icons">lock </span>  <input type="password" placeholder="SENHA">
             <input type="submit" value="ENTRAR">
        </div>
   </div> #}

   <div class="card table-container">
        <a href="/teachers/create"> Novo</a>
       <table width="100%">
              <thead > {# Cabeçalho #}
                   
                     <tr>{# Linha #}
                          
                         <th>Teacher</th>
                         <th>Matérias</th>
                         <th>Ação</th>
                     </tr>
              </thead>
              <tbody> {# corpo da tabela #}
              {% for teacher in teachers %}
                     <tr>

                       <td>
                          <span 
                          
                                style="background-image: url({{teacher.avatar_url}})">
                          
                          </span>
                          {{teacher.name}}
                      </td> {# coluna da tabela, nome e foto #}
                      
                      
                      <td>
                            {{teacher.services}}
                      </td>{# serviços #}

                      
                       <td> 
                       
                            <a href="/teachers/{{teacher.id}}">Ver</a>

                           <form  action="/teachers?_method=DELETE" method="post">
                                <input type="hidden" name="id" value={{teacher.id}} >
                                <input type="submit" value="Deletar">
                           </form>
                            
                       </td>{# botões de ação #}

                   
                     </tr>
               {%endfor%}      
             </tbody>
       </table>
   
   
   
   </div>
{% endblock content%}
